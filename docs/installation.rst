============
Installation
============

At the command line either via easy_install or pip::

    $ easy_install mozilla-schema-creator
    $ pip install mozilla-schema-creator

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv mozilla-schema-creator
    $ pip install mozilla-schema-creator
